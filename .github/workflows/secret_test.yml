name: Secret Test

on:
  workflow_dispatch:

jobs:
  secret-test:
    runs-on: ubuntu-latest
    steps:
      - name: Attempt to print secret
        run: echo "The token is ${{ secrets.MY_SENSITIVE_TOKEN }}"
